## 初始化仓库

# 进入你的项目目录
cd your-project-directory

# 初始化 Git 仓库（如果还没有初始化）
git init

# 添加特定文件夹到缓存区
git add my_folder
git add .

# 提交文件到本地仓库
git commit -m "Add my_folder"

# 链接远程仓库
git remote add origin https://github.com/HobbitWinnie/Jobcodes.git

# 推送文件夹到 GitHub
git push -u origin master